# 기말 프로젝트 자기 평가서 (self_review.md)

이번 파이썬 프로그래밍 기말 프로젝트로 나만의 가계부 패키지를 직접 설계하고 완성하면서 느끼고 배운 점들을 솔직하게 정리해 보았습니다.

---

## 1. 내가 가장 잘했다고 생각하는 부분 (성취한 점)
이번 가계부 및 예산 추적기 패키지를 만들면서 가장 보람찼던 부분은 **클래스 상속 구조를 제대로 이해하고 실무적으로 적용해 본 것**입니다. 수업 시간에 이론으로만 배웠던 부모 클래스(`Transaction`)와 자식 클래스(`Income`, `Expense`)의 관계를 직접 설계하면서, `super().__init__()`을 통해 부모의 초기화 로직을 그대로 이어받고 자식 클래스만의 특징을 덧붙이는 구조를 명확하게 다룰 수 있게 되었습니다. 

또한 단순히 구동만 되는 스크립트가 아니라, `pycodestyle` 스타일 검사 도구를 사용해 가로 길이 제한과 공백 규정을 맞춰가며 경고를 단 1건도 남기지 않고 해결해 냈을 때 큰 뿌듯함을 느꼈습니다. `pytest`를 통해 정상 작동뿐만 아니라 빈 거래 리스트 같은 예외적인 상황(엣지 케이스)까지 꼼꼼히 점검해 보는 과정을 거치면서 코드의 품질과 신뢰성에 대해 깊이 고민해 보는 좋은 계기가 되었습니다.

---

## 2. 조금 아쉬웠던 부분 (어려웠던 점이나 성찰)
가장 아쉬웠던 부분은 패키지 내부 모듈 간의 **순환 참조(Circular Dependency) 문제 해결과 다형성 구현** 사이에서의 타협이었습니다. 가계부 데이터의 수입과 지출 유형을 구별해 통계를 내는 `utils.py`를 작성할 때, 자식 클래스인 `Income`과 `Expense`를 직접 임포트해 객체 타입을 비교하고 싶었으나 이 과정에서 임포트가 꼬이는 순환 참조 에러를 겪었습니다. 

이를 해결하기 위해 각 객체를 딕셔너리로 변환한 뒤 내부 텍스트 값(`type == "income"`)을 비교하는 방식을 취했습니다. 에러 없이 깔끔하게 동작하도록 구현하긴 했으나, 파이썬 객체지향 고유의 `isinstance`와 같은 다형성 이점을 완벽히 살리는 설계를 더 우아하게 설계해 내지 못해 개인적으로는 조금 아쉬움이 남습니다.

---

## 3. 다음에 더 개선해 보고 싶은 점 (향후 학습 계획)
지금 작성된 가계부는 프로그램이 종료되면 입력했던 모든 거래 데이터가 휘발되어 사라집니다. 다음에는 파일 입출력(CSV 나 JSON 파일 연동) 기능을 추가해서, 사용자가 입력한 가계부 데이터가 컴퓨터에 텍스트 파일로 영구 저장되고 다시 프로그램을 켰을 때 불러올 수 있는 실제 데이터베이스 연동형 프로그램을 짜보고 싶습니다. 

그리고 보너스 요건에 언급된 Sphinx를 활용한 자동 문서화나 깃허브 액션(GitHub Actions)을 공부하여, 제가 작성한 코드가 깃허브에 올라갈 때마다 자동으로 코드 스타일과 작동 테스트가 수행되는 빌드 자동화 시스템(CI/CD)을 꼭 한 번 다뤄보고 싶습니다.

---

## 4. AI 도구 사용 정책 준수 및 윤리적 소명 (Gemini 사용기)
- **사용 도구**: 구글 Gemini (인공지능 보조 멘토)
- **주도적 활용 과정 서술**:
  이번 기말 프로젝트를 진행하면서 파이썬의 표준 패키지 구조를 구성하는 법과 복잡한 스타일 경고를 해결하기 위해 구글 Gemini의 자문을 받았습니다. 코드의 전체적인 흐름과 클래스 상속 뼈대는 가이드라인을 보며 직접 구현해 나갔으나, 스타일 정적 분석 도구인 `pycodestyle`을 실행했을 때 겪었던 `W292(newline 누락)`이나 `W293(빈 줄에 든 공백)` 같은 생소한 포맷 경고들의 구체적인 원인과 VS Code 설정법(`trim trailing whitespace`)에 대해 질문하여 해결 방안을 명확히 배웠습니다.
  
  또한, 단위 테스트 작성 시 제가 지출 합계를 암산하는 과정에서 덧셈 계산 실수를 저질러 `pytest`가 실패했을 때, Gemini가 모의 거래 데이터들의 덧셈 오류를 정확히 짚어주어 테스트 기대값을 바로잡을 수 있었습니다. Gemini가 제공한 안내와 가이드는 모두 제가 한 줄 한 줄 원리를 스스로 설명하고 이해할 수 있는 범위 내에서만 정직하게 보조적으로 사용하였으며, 이를 통해 프로그래밍 개발과 검증 프로세스의 깊이를 성실히 학습했음을 명확히 소명합니다.